# 产品需求文档（PRD）

## 1. 文档信息
- 产品名称：Yido
- 版本：v1.0
- 日期：2026-08-16
- 适用对象：办公室工作人员、学生、普通个人用户
- 目标平台：本地桌面端 / Web 轻应用
- 使用范围：单人使用，不面向团队协作
- 项目定位：本地优先、隐私优先、离线可用、轻量高效

## 2. 产品定位
Yido 是一款面向个人用户的本地任务管理应用，帮助用户在不依赖云端、不需要团队协作、不需要多人账号的前提下，快速记录、安排和完成日常待办事项。该产品的核心价值包括：
- 简单易用
- 本地存储
- 保护隐私
- 离线可用
- 稳定可靠

目标用户包括：
- 办公室工作人员：管理工作待办、会议安排、截止事项和待跟进事项
- 学生：管理课程作业、复习计划、考试安排和学习任务
- 其他个人用户：管理生活事务、购物清单、健康计划、学习目标等

## 3. 产品目标
### 3.1 商业目标
- 为个人用户提供一款轻量、低门槛的任务管理工具
- 解决“仅需处理自己的待办，不需要复杂协作”的真实需求
- 提供稳定且高频使用的个人任务清单解决方案

### 3.2 用户目标
- 在几秒内创建一条任务
- 在一个视图中快速判断今天需要处理的事项
- 在无网络环境下仍可正常使用
- 在不注册、不绑定云账号的情况下即可使用
- 能清晰看到待办、已过期、已完成的状态

### 3.3 产品价值主张
1. 本地优先：数据保存在用户本地设备，不依赖云同步
2. 个人专属：仅面向单用户，不支持团队协作或多人账号
3. 隐私优先：不采集无关数据，不要求第三方账号
4. 简洁高效：减少复杂企业功能，聚焦任务管理本身
5. 离线可靠：网络中断时仍可处理待办
6. 长期稳定：适合长期使用，学习成本低

## 4. 用户画像
### 4.1 办公室工作人员
- 需要管理工作待办、会议纪要、截止事项和跟进事项
- 重视任务优先级、提醒和视图清晰度
- 偏好轻量工具而不是复杂项目管理软件

### 4.2 学生
- 需要管理作业、复习计划、考试安排和学习任务
- 更偏好按日期、标签和状态组织任务
- 需要低门槛、自控且本地化的工具

### 4.3 个人用户
- 需要管理生活事务、健康计划、学习目标和家庭事务
- 更看重操作简单、隐私保护和界面美观
- 不需要团队协作功能

## 5. 产品范围
### 5.1 在范围内
- 任务创建、编辑、删除、完成、恢复
- 任务标题、描述、截止日期、截止时间、优先级、标签
- 今日、待办、已过期、将来、已完成、全部任务视图
- 搜索与筛选（关键词、标签、优先级、状态）
- 本地存储与本地备份恢复
- 本地提醒与通知
- 默认视图、主题、语言、提醒提前时间等设置
- 任务重复规则：每日、每周、每月

### 5.2 不在范围内
- 云同步服务
- 团队协作功能
- 多用户账号体系
- 多人共享清单
- 项目分工、工时统计、报表分析
- 组织架构、部门管理
- 企业审批、权限管理

## 6. 业务场景
### 6.1 场景一：快速记录任务
- 用户打开应用，输入任务标题
- 可选择添加描述、优先级、标签、截止时间、提醒时间
- 保存后任务立即出现在主列表中

### 6.2 场景二：查看今天安排
- 用户进入“今日”视图
- 查看当天待办和已逾期任务
- 快速判断今天的重点工作

### 6.3 场景三：完成任务
- 用户在任务卡片上点击完成
- 任务移入已完成区域
- 用户以后可以恢复或删除

### 6.4 场景四：筛选与搜索
- 用户根据关键词、标签、优先级或状态搜索任务
- 快速缩小查看范围

### 6.5 场景五：本地备份恢复
- 用户导出任务备份文件
- 在新设备或新环境下导入恢复
- 不依赖云端服务

## 7. 用户需求
- 快速输入任务，尽量少步骤
- 一次看清今天需要处理的事情
- 按优先级和截止时间管理任务
- 可按标签、关键词、状态过滤任务
- 可脱网继续使用
- 数据保存在本地，不泄露隐私
- 界面简洁、视觉舒适、不造成干扰

## 8. 产品原则
1. 以个人使用场景为核心，不偏向企业协作
2. 以本地化和隐私优先为基础
3. 功能聚焦，不扩展为复杂项目管理工具
4. 操作尽量简单，界面尽量轻，不制造理解负担
5. 稳定性优先于功能复杂度
6. 所有功能都必须在单人场景中有明确价值

## 9. 用户故事
### 9.1 任务录入
- 作为用户，我希望几秒内就能记录一条任务，以便抓住待办事项。
- 作为用户，我希望可以设置优先级和截止时间，以便安排工作和学习。

### 9.2 任务管理
- 作为用户，我希望能编辑任务内容，以便修正错误状态。
- 作为用户，我希望能完成任务，并在需要时恢复。
- 作为用户，我希望能删除任务，避免误操作造成困扰。

### 9.3 视图与筛选
- 作为用户，我希望查看“今日”和“待办”视图，以便提升效率。
- 作为用户，我希望按标签和优先级筛选任务，以便快速聚焦。

### 9.4 提醒与备份
- 作为用户，我希望设置提醒时间，以便在截止前收到提醒。
- 作为用户，我希望数据保存在本地，确保隐私安全。
- 作为用户，我希望能导出和恢复备份，降低数据风险。

## 10. 功能需求
### 10.1 任务模型
任务至少应包含以下字段：
- id：唯一标识
- title：标题，必填
- description：详细描述，可选
- status：状态（todo / in_progress / completed）
- priority：优先级（low / normal / high）
- tags：标签列表
- due_date：截止日期
- due_time：截止时间
- remind_at：提醒时间
- created_at：创建时间
- updated_at：更新时间
- completed_at：完成时间
- repeat_rule：重复规则（none / daily / weekly / monthly）

### 10.2 任务管理
- 新增任务
- 编辑任务
- 删除任务
- 标记完成
- 恢复已完成任务
- 设置优先级、标签和提醒时间
- 支持按创建时间、截止时间、优先级排序
- 支持重复任务

### 10.3 视图
- 今日视图
- 待办视图
- 已过期视图
- 将来视图
- 已完成视图
- 全部任务视图
- 按标签聚合视图

### 10.4 搜索与筛选
- 按关键词搜索
- 按标签筛选
- 按优先级筛选
- 按状态筛选
- 按日期范围筛选
- 支持组合筛选

### 10.5 提醒功能
- 支持任务提醒时间设置
- 支持本地通知、桌面弹窗、应用内提醒
- 支持延后提醒和关闭提醒
- 提醒仅在当前设备生效

### 10.6 本地存储与备份
- 数据默认保存在本地数据库或文件
- 程序启动和关闭时需保证数据写入可靠
- 支持导出备份
- 支持导入备份恢复数据
- 支持崩溃恢复和基本错误处理

### 10.7 设置与个性化
- 主题切换：浅色 / 深色 / 自动
- 默认视图设置
- 每日开始时间设置
- 默认提醒提前时间
- 语言设置
- 本地偏好配置，仅影响当前设备

## 11. 非功能性要求
### 11.1 性能
- 任务量达到 5000 条时，仍保持流畅操作
- 搜索和筛选响应时间控制在 500ms 内
- 基本操作反馈在 1 秒内完成

### 11.2 稳定性
- 异常退出后不导致大规模数据丢失
- 启动时检查数据状态并尝试恢复
- 备份和数据文件具备基础校验能力

### 11.3 隐私与安全
- 不收集云端数据
- 不依赖第三方账号
- 数据仅保存在用户设备
- 导出文件由用户自行管理和识别

### 11.4 可用性
- 学习成本低，不需复杂培训
- 支持键盘和鼠标操作
- 适合非技术用户使用
- 基础功能无需文档即可完成

## 12. 数据库设计
### 12.1 技术选择
采用 SQLite 作为本地数据库，配合 Tauri 封装桌面应用，前端使用 React + TypeScript 构建界面。

### 12.2 数据库结构
#### 12.2.1 tasks 表
```sql
CREATE TABLE tasks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  description TEXT DEFAULT '',
  status TEXT NOT NULL DEFAULT 'todo' CHECK(status IN ('todo', 'in_progress', 'completed')),
  priority TEXT NOT NULL DEFAULT 'normal' CHECK(priority IN ('low', 'normal', 'high')),
  due_date TEXT DEFAULT NULL,
  due_time TEXT DEFAULT NULL,
  remind_at TEXT DEFAULT NULL,
  repeat_rule TEXT DEFAULT NULL CHECK(repeat_rule IN ('none', 'daily', 'weekly', 'monthly')),
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  completed_at TEXT DEFAULT NULL,
  is_deleted INTEGER NOT NULL DEFAULT 0,
  archived_at TEXT DEFAULT NULL,
  sort_order INTEGER NOT NULL DEFAULT 0
);
```

#### 12.2.2 tags 表
```sql
CREATE TABLE tags (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL UNIQUE,
  color TEXT DEFAULT '#5B8DEF',
  created_at TEXT NOT NULL
);
```

#### 12.2.3 task_tags 表
```sql
CREATE TABLE task_tags (
  task_id INTEGER NOT NULL,
  tag_id INTEGER NOT NULL,
  PRIMARY KEY (task_id, tag_id),
  FOREIGN KEY (task_id) REFERENCES tasks(id) ON DELETE CASCADE,
  FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE
);
```

#### 12.2.4 reminders 表
```sql
CREATE TABLE reminders (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  task_id INTEGER NOT NULL,
  remind_at TEXT NOT NULL,
  remind_type TEXT NOT NULL DEFAULT 'notification' CHECK(remind_type IN ('notification', 'popup', 'in_app')),
  status TEXT NOT NULL DEFAULT 'pending' CHECK(status IN ('pending', 'sent', 'dismissed', 'expired')),
  created_at TEXT NOT NULL,
  sent_at TEXT DEFAULT NULL,
  FOREIGN KEY (task_id) REFERENCES tasks(id) ON DELETE CASCADE
);
```

#### 12.2.5 settings 表
```sql
CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
```

#### 12.2.6 backups 表
```sql
CREATE TABLE backups (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  file_name TEXT NOT NULL,
  file_path TEXT NOT NULL,
  created_at TEXT NOT NULL,
  size_bytes INTEGER NOT NULL DEFAULT 0,
  note TEXT DEFAULT ''
);
```

#### 12.2.7 task_events 表（可选增强）
```sql
CREATE TABLE task_events (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  task_id INTEGER NOT NULL,
  event_type TEXT NOT NULL CHECK(event_type IN ('created', 'updated', 'completed', 'reopened', 'deleted', 'restored')),
  old_value TEXT DEFAULT NULL,
  new_value TEXT DEFAULT NULL,
  created_at TEXT NOT NULL,
  FOREIGN KEY (task_id) REFERENCES tasks(id) ON DELETE CASCADE
);
```

### 12.3 数据库设计说明
- tasks 是核心表，存储任务主数据
- tags 和 task_tags 支持任务多标签管理
- reminders 独立负责本地通知调度
- settings 负责应用偏好设置
- backups 支持导出和恢复
- task_events 为未来扩展提供审计能力

## 13. 架构与实现方案
### 13.1 技术栈
- 前端：React + TypeScript
- 桌面应用壳：Tauri
- 本地数据库：SQLite
- 状态管理：Zustand
- 路由：React Router
- 样式：Tailwind CSS
- 通知：系统原生通知 / 应用内提醒
- 测试：Vitest + Testing Library

### 13.2 技术方案说明
#### React + TypeScript
- 负责界面与交互层
- 适合做现代、清晰、轻量化的任务管理界面
- 强组件化能力，便于模块拆分

#### Tauri
- 用于封装桌面端应用
- 启动快、资源占用低、可访问本机文件系统和通知能力
- 更适合本地工具而不是重型企业应用

#### SQLite
- 适合单用户本地数据存储
- 稳定、轻量、无需单独部署数据库服务
- 非常适合个人任务管理场景

## 14. 功能模块拆分与验收标准
### 14.1 模块一：快速新增任务
职责：
- 快速录入待办任务
- 支持标题、描述、优先级、标签和截止时间

交互：
- 顶部浮动输入框
- 用户输入标题后可一键保存
- 展开高级选项时可填写备注和截止时间

验收标准：
- 标题为空时禁止保存，并给出提示
- 任务保存成功后立即显示在任务列表中
- 支持标签添加与删除
- 优先级可选择 low / normal / high
- 截止时间和提醒时间可设置
- 保存失败时提示明确错误信息
- 交互时存在轻微的滑入和反馈动画

### 14.2 模块二：今日工作台
职责：
- 展示今日待办情况
- 展示已过期事项
- 给出今日重点任务概览

交互：
- 顶部显示“今日 / 本周 / 已逾期”切换
- 今日任务用卡片方式显示

验收标准：
- 今日视图默认展示当天任务和已逾期任务
- 任务按优先级和截止时间排序
- 已逾期任务用醒目颜色标识
- 完成任务后从今日列表中移除
- 空状态提示清晰且不影响页面整体美观
- 切换视图时带轻微淡入动画

### 14.3 模块三：任务列表
职责：
- 展示任务总览
- 支持状态筛选、标签筛选、优先级筛选和排序

交互：
- 顶部筛选器和搜索框
- 切换全部、待办、已完成、已过期

验收标准：
- 能按状态筛选全部/待办/已完成/已过期
- 能按标签筛选任务
- 能按优先级筛选任务
- 能按截止时间或创建时间排序
- 过滤为空时展示明确空状态
- 列表刷新后保持流畅体验

### 14.4 模块四：任务详情与编辑
职责：
- 查看任务内容
- 修改任务属性
- 更新状态和提醒

交互：
- 点击任务卡片后右侧滑出详情面板
- 允许编辑标题、描述、标签和截止时间

验收标准：
- 点击任务可打开详情面板
- 修改后成功保存并立即更新列表
- 标题为空时禁止保存
- 完成任务后状态同步更新
- 恢复已完成任务后返回未完成列表
- 操作过程中发生异常时给出明确提示
- 面板打开/关闭带平滑动画

### 14.5 模块五：搜索与过滤
职责：
- 支持关键词搜索
- 支持组合筛选

交互：
- 顶部搜索框实时响应
- 可筛选状态、优先级、标签和日期范围

验收标准：
- 支持标题和描述关键字搜索
- 支持标签和状态组合筛选
- 过滤结果为空时展示“无匹配任务”
- 搜索输入时响应在 500ms 内完成
- 搜索框聚焦和失焦有状态反馈

### 14.6 模块六：提醒与通知
职责：
- 设置任务提醒时间
- 触发本地通知
- 支持延后或忽略提醒

交互：
- 提醒设置在任务编辑中完成
- 通知弹出后可直接查看任务详情

验收标准：
- 允许设置截止前 1 天 / 2 天 / 1 周等常用提醒
- 提醒时间不能早于任务创建时间
- 到达提醒时间后发出系统通知或应用内提醒
- 用户可延后提醒或关闭提醒
- 提醒状态会在任务中显示
- 严重情况下电子通知失败时自动回退到应用内提醒

### 14.7 模块七：设置与备份
职责：
- 配置主题、默认视图、提醒提前时间等设置
- 导出和导入本地备份

交互：
- 设置页采用简洁卡片式布局
- 导出/导入操作通过确认框和成功提示完成

验收标准：
- 主题切换即时生效
- 默认视图持久化到本地配置
- 提醒提前时间保存成功
- 备份文件可导出到指定目录
- 导入备份后恢复数据并展示提示
- 删除或恢复数据前进行确认提示
- 设置页面支持浅色/深色/自动模式

## 15. 界面与交互设计
### 15.1 页面结构
建议采用三栏布局：
- 左侧：导航栏
- 中间：任务列表与筛选区
- 右侧：详情编辑面板

整体布局要求：
- 视觉简洁，不拥挤
- 任务卡片清晰可读
- 状态色标识明确
- 交互动作轻盈自然

### 15.2 页面推荐内容
#### 首页 / 今日视图
- 顶部统计区：今日任务、已逾期、已完成
- 任务卡片列表
- 快速添加任务入口

#### 待办视图
- 任务清单
- 状态和优先级筛选
- 可直接完成/编辑

#### 设置视图
- 主题设置
- 提醒设置
- 数据导出导入
- 语言设置

### 15.3 动效设计要求
- 任务卡片 hover 时轻微上浮并增加阴影
- 新任务添加时从底部滑入
- 完成时出现勾选动画和颜色变化
- 删除时弹出确认提示，不直接执行危险操作
- 面板切换采用平滑滑入/滑出动画
- Toast 提示采用淡入 + 上移 + 自动消失

动效原则：
- 保持轻柔，不要过度使用震动或夸张动画
- 动效服务于信息层级而不是干扰用户

## 16. 非功能性要求补充
### 16.1 易用性
- 用户在首次使用时可在 5 分钟内掌握核心操作
- 关键功能默认可见，不需要额外学习成本

### 16.2 稳定性
- 所有数据库写入操作应具备事务处理
- 数据修改后应异步保存，不阻塞 UI
- 执行失败时有明确回滚或提示

### 16.3 本地化
- 默认语言为中文或系统语言
- 不依赖外部服务进行核心功能
- 所有配置保存在本地

## 17. 风险与限制
- 本地数据安全依赖设备自身保护，需完善备份方案
- 若系统通知不支持，需自动退化为应用内提醒
- 当前版本不提供多端同步，不支持多人协作
- 功能扩展过多可能破坏工具本身的轻量化定位

## 18. 版本规划
### v1.0（MVP）
- 任务创建、编辑、删除、完成
- 今日视图、待办视图、已完成视图
- 标签和优先级
- 提醒与通知
- 本地备份与恢复

### v1.1
- 更完善的筛选与排序
- 重复任务支持
- 更稳定的数据恢复和操作记录

### v1.2+
- 更强的自定义设置
- 归档和历史管理
- 更丰富的提醒规则和高级筛选

## 19. 验收总表
| 模块 | 核心需求 | 验收标准 |
| --- | --- | --- |
| 快速新增任务 | 快速创建任务 | 标题不能为空，保存成功后立即显示，支持标签和截止时间 |
| 今日工作台 | 关注今天重点 | 任务按时间和优先级排序，逾期任务高亮 |
| 任务列表 | 管理全部任务 | 可筛选、排序、显示空状态 |
| 详情编辑 | 更新任务信息 | 可修改标题、描述、标签、截止时间，修改后即时更新 |
| 搜索与过滤 | 快速定位 | 支持关键词/状态/标签/优先级搜索 |
| 提醒通知 | 提前处理任务 | 提醒触发本地通知，可延后和忽略 |
| 设置与备份 | 数据保护与配置 | 支持主题、备份、导入导出、默认视图配置 |

## 20. 结论
Yido 的本质是一个“本地优先、单人使用、轻量高效”的个人任务管理应用。它不追求团队协作和云端能力，而是通过清晰的任务管理流程、稳定的本地数据存储、自然的提醒机制和简洁的桌面 UI，帮助用户高效完成日常待办管理。

在实现上，推荐采用 React + TypeScript + Tauri + SQLite 的方案，既满足当前 PRD 的全部需求，也为后续扩展预留空间。该方案符合本地隐私要求、离线使用要求和单人任务管理的实际场景，适合成为本项目的正式技术落地方案。

## 21. 需求摘要
- 目标用户：个人用户、学生、办公室工作人员
- 核心功能：任务管理、提醒、视图、筛选、备份恢复
- 数据模式：本地优先，单用户
- 禁止能力：云同步、团队协作、多用户账号
- 产品定位：轻量高效、私密可靠、离线可用
- 推荐技术栈：React + TypeScript + Tauri + SQLite
- 设计原则：极简、清晰、舒适、低干扰
